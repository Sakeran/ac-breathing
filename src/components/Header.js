import "./Header.css";

const Header = () => (
  <header>
    <div className="logo">Breathing</div>
    <button className="header-about-btn" id="header-about-btn">
      ?
    </button>
  </header>
);

export default Header;
